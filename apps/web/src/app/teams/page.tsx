import { PlaceholderPage } from "@/components/placeholder-page";

export default function TeamsPage() {
  return (
    <PlaceholderPage
      eyebrow="Team intelligence"
      title="Teams"
      description="Search every team and open a unified depth view combining its backfield, target hierarchy, and role changes."
    />
  );
}

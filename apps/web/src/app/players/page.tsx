import { PlaceholderPage } from "@/components/placeholder-page";

export default function PlayersPage() {
  return (
    <PlaceholderPage
      eyebrow="Phase 3"
      title="Players"
      description="Player search and individual evidence pages will connect each finding to its underlying role history."
    />
  );
}

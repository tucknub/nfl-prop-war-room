import { PlaceholderPage } from "@/components/placeholder-page";

export default function MethodologyPage() {
  return (
    <PlaceholderPage
      eyebrow="Phase 3"
      title="Methodology"
      description="Plain-language definitions will describe the existing Python authority without recreating its calculations in the frontend."
    />
  );
}

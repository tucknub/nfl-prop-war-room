import { PlaceholderPage } from "@/components/placeholder-page";

export default function DataStatusPage() {
  return (
    <PlaceholderPage
      eyebrow="Phase 3"
      title="Data status"
      description="Publishing freshness, validation state, and source-build metadata will appear here when deterministic web bundles are connected."
    />
  );
}
